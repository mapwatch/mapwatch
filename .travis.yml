language: node_js

install: 
  - npm install -g elm
  - elm --version
  - travis_retry elm-package install --yes

script:
  - ./build.sh

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist/
  github_token: $GITHUB_TOKEN
  #keep_history: true
  on:
    branch: master
