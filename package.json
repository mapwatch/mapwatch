{
  "name": "mapwatch",
  "version": "1.0.0",
  "description": "Automatically time your Path of Exile map clears. Electron version.",
  "main": "electron/main.js",
  "scripts": {
    "_electron-packager": "yarn _build:electron && electron-packager dist/electron mapwatch --platform=$PLATFORM --arch=$ARCH --out=dist",
    "_build:electron": "./build.sh && rm -rf dist/electron && mkdir -p dist/electron/dist && cp -rp dist/www dist/electron/dist/www && cp -rp electron package.json ./dist/electron",
    "build:linux": "PLATFORM=linux ARCH=x64 yarn _electron-packager",
    "build:win32": "PLATFORM=win32 ARCH=x64 yarn _electron-packager",
    "start": "./build.sh && electron ."
  },
  "repository": "https://github.com/mapwatch/mapwatch",
  "author": "Evan Rosson",
  "license": "GPL-3.0",
  "devDependencies": {
    "electron": "^2.0.0",
    "electron-packager": "^12.1.0"
  }
}
