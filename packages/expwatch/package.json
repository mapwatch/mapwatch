{
  "name": "expwatch",
  "version": "1.0.0",
  "main": "main.js",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "dev": "rm -rf .dev && mkdir .dev && cp ./src/main.js .dev && elm-make src/Main.elm --output=.dev/elm.js",
    "start": "yarn dev && node .dev/main.js"
  },
  "devDependencies": {
    "elm": "^0.18.0"
  }
}
