{
  "name": "@mapwatch/www",
  "version": "1.0.0",
  "main": "build/elm.js",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "hello": "echo foo; echo bar; echo $SHELL; echo %SHELL%",
    "_build:version": "bash scripts/_build/version",
    "_build:elm": "elm make src/Main.elm --optimize --output=build/elm.js",
    "build": "yarn _build:version && elm-app build && yarn _build:elm",
    "start": "yarn _build:version && bash -c \"ELM_DEBUGGER=false elm-app start\"",
    "//test": "elm-app test",
    "clean": "rm -rf build public/rss.xml public/CHANGELOG.md public/version.txt"
  },
  "dependencies": {
    "@mapwatch/lib": "1.0.0",
    "@mapwatch/datamine": "1.0.0",
    "@mapwatch/rss": "1.0.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.11.0",
    "create-elm-app": "^4.1.2",
    "rimraf": "^3.0.0",
    "sakura.css": "^1.0.0",
    "scripty": "^1.7.2"
  }
}
