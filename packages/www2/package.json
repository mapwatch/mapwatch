{
  "name": "@mapwatch/www2",
  "version": "1.0.0",
  "main": "dist/elm.js",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "_build:version": "scripty",
    "build": "yarn _build:version && elm-app build",
    "start": "yarn _build:version && ELM_DEBUGGER=false elm-app start",
    "//test": "elm-app test",
    "clean": "elm-app clean"
  },
  "dependencies": {
    "@mapwatch/lib": "1.0.0",
    "@mapwatch/rss": "1.0.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.11.0",
    "create-elm-app": "^3.0.3",
    "rimraf": "^2.6.2",
    "sakura.css": "^1.0.0",
    "scripty": "^1.7.2"
  }
}
