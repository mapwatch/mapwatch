{
  "name": "@mapwatch/lib",
  "version": "1.0.0",
  "main": "dist/main.js",
  "license": "GPL-3.0",
  "private": true,
  "dependencies": {
    "@mapwatch/www": "1.0.0"
  },
  "devDependencies": {
    "elm": "^0.18.0"
  },
  "scripts": {
    "postinstall": "elm-package install --yes",
    "demo-raw-elm": "yarn build && node ./demo-raw-elm.js",
    "demo": "yarn build && node ./demo.js",
    "build": "rm -rf dist && mkdir -p dist && elm-make Main.elm --output=dist/elm.js && cp main.js dist/"
  }
}
